# Changelog

## Phase A — Foundation (2026-03-18 → 2026-03-19)

### New files
- `.env.example` — complete configuration template with comments for every variable
- `config/caddy/Caddyfile` — Caddy reverse proxy, auto-TLS via Let's Encrypt DNS-01, iframe embedding support
- `config/caddy/Dockerfile` — custom Caddy image built with caddy-dns/namedotcom plugin (xcaddy)
- `config/alertmanager/alertmanager.yml.template` — full Slack + email template, generated by install.sh via envsubst
- `planning/phase-a-notes.md` — architecture decisions, service list, open issues
- `planning/changelog.md` — this file
- `README.md` — rewritten: quickstart + port forwarding guide

### Modified files
- `docker-compose.yml`
  - Added: Caddy (reverse proxy, `production` profile, port 8443), DuckDNS (dynamic IP, `production` profile), Watchtower (monitor-only)
  - Caddy: builds from `config/caddy/Dockerfile` (custom image with namedotcom DNS plugin)
  - Added: `grafana-data` named volume so Grafana SQLite DB (alert rules, user edits) persists across restarts
  - Added: `alertmanager-data` named volume
  - Added: `caddy_data`, `caddy_config` volumes for TLS cert persistence
  - Prometheus: added `--storage.tsdb.retention.time` and `--storage.tsdb.retention.size` from env vars
  - Grafana: added anonymous access env vars, allow-embedding, SMTP config, admin creds from env
  - Grafana: `GF_SERVER_DOMAIN` hardcoded to `fridge.zickers.us` (avoids port suffix from DOMAIN var)
  - All services: `restart: unless-stopped`
  - Compose profiles: `production` profile for caddy + duckdns (not started in CI)

- `install.sh` — full rewrite
  - Idempotent (safe to re-run)
  - Coloured output, step headers
  - Preflight: Docker, Compose plugin, envsubst
  - Validates .env (required vars, stricter in production than CI)
  - Generates `alertmanager.yml` from template via envsubst
  - Re-reads env vars with special characters (e.g. `!`) directly from .env file before docker compose runs
  - ufw firewall rule for port 9091 using `insert 1` (allow before deny, correct ordering)
  - Starts stack with `--profile production` in production, without in CI
  - Health check polling for all 4 core services
  - Creates Grafana lab user via API (production only)
  - Prints summary with URLs and Pushgateway address

- `config/grafana/provisioning/dashboards/dashboard-provider.yml`
  - `disableDeletion: false` (was true)
  - `allowUiUpdates: true` (was false)

- `.github/workflows/ci.yml`
  - Installs `gettext` (provides envsubst)
  - Creates a minimal `.env` with test credentials before running install.sh
  - Calls `./install.sh` (CI=true auto-detected from GitHub Actions env)

- `.gitignore`
  - Removed ddclient entry (ddclient replaced by DuckDNS container)

- `.env.example`
  - Added: `DUCKDNS_SUBDOMAINS`, `DUCKDNS_TOKEN`, `NAMEDOTCOM_USER`, `NAMEDOTCOM_API_TOKEN`
  - Added: `PROMETHEUS_RETENTION_TIME`, `PROMETHEUS_RETENTION_SIZE`
  - `DOMAIN` / `GRAFANA_PUBLIC_URL` updated to `fridge.zickers.us:8443`
  - `LAB_USER_NAME` quoted to handle spaces in value

### Removed
- `config/ddclient/` directory and `ddclient.conf.template` — replaced by DuckDNS container

### Infrastructure decisions made
- Hostname: `fridge.zickers.us:8443` (subdomain, port 8443 to avoid conflict with UniFi Direct Connect on 443)
- TLS: DNS-01 challenge via name.com API (caddy-dns/namedotcom plugin) — no port 80 or 443 required
- DynDNS: linuxserver/duckdns container keeps `zickers-fridge.duckdns.org` updated; name.com ANAME → duckdns
- Email relay: SendGrid SMTP (free tier, 100/day) — credentials pending
- UFW: port 9091 allowed from college CIDR `128.119.0.0/16` only; port 8443 open to all via router
- Local network (192.168.1.0/24) also allowed on 9091 for local testing

### Fridge-data-uploader changes
- `example_data/collectall.py`: fixed path bug — was looking for `.env` in `example_data/` instead of repo root
- `server.env`: updated PUSHGATEWAY_URL from dead EC2 address to `zickers-fridge.duckdns.org:9091`
- `push_metrics.py`: push failure log messages now include the target URL

---

## Live deployment status (2026-03-19)

- Stack running on local Arch Linux machine (192.168.1.134), pending college IT server
- `https://fridge.zickers.us:8443` serving Grafana over valid HTTPS ✓ (Let's Encrypt cert via name.com DNS-01)
- Manny (Bluefors) pushing real data to Pushgateway ✓
- Dodo pushing data ✓ (unexpected — uploader must already be running on Dodo)
- Data visible in Grafana for both fridge-manny and fridge-dodo ✓
- Watchtower: crash-looping (SMTP not configured yet — not critical)

---

## Pending / next steps

### TLS (immediate)
- Wait for Let's Encrypt rate limit to clear (~22:11 UTC 2026-03-19), Caddy retries automatically
- Verify cert obtained: `docker compose logs caddy | grep "certificate obtained"`

### Phase B — Alert infrastructure
- Wire Alertmanager receivers: Slack (already templated) + email (SendGrid — need API key)
- Add Grafana unified alerting contact points (provisioned via config)
- Replace synthetic test alert rule in `config/prometheus/alerts.yml` with real infra alert (`up == 0`)
- `docs/managing-alerts.md` — non-technical guide for lab members

### Phase C — Uploader / fridge configs
- Run `collectall.py` on Sid to get channel layout → create `fridge_configs/sid.config`
- Create `fridge_configs/manny.config` based on confirmed channel list
- Redesign `push_metrics.py` to be config-driven
- Point Sid at new Pushgateway URL

### Phase D — Dashboards (live data now available — can start)
- fridge-overview.json: cross-fridge temperature panel, log-scale Y axis (50K → 10mK)
- fridge-pressures.json: all maxigauge channels per fridge
- fridge-manny-detail.json: all Manny channels, heaters, flow, valves
- fridge-sid-detail.json, fridge-dodo-detail.json (placeholder)

### Phase E — Robustness & docs
- Caddy TLS confirmed in production
- OS auto-update docs (Ubuntu `unattended-upgrades` for eventual college server)
- Stack self-monitoring: Prometheus alert on `up == 0` → email
- `docs/` folder: server-setup, managing-alerts, adding-a-fridge, panel-embedding, troubleshooting
