{
  "settings": [
    {
      "provider": "name.com",
      "domain": "${DDNS_OWNER}.${DDNS_DOMAIN}",
      "username": "${NAMEDOTCOM_USER}",
      "token": "${NAMEDOTCOM_API_TOKEN}",
      "ttl": 300,
      "ip_version": "ipv4"
    }
  ]
}
